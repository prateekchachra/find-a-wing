import React, { Component } from 'react'
import PulseLoader from 'react-native-pulse-loader';




export default class Loader extends Component {
    render() {
        return (
           <PulseLoader avatar={user.avatar}
           />
        )
    }
}
